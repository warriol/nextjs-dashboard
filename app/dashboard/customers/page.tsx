import React from 'react';

export default async function CustomerPage() {

    // concetar a una API externa
    /*
    const res = await fetch('https://api.example.com/customers');
    const data = await res.json();
    console.log(data);
    */

    return (
        // Your page content goes here
        <p>Customers</p>
    );
}
